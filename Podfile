# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Measure Tracker' do
  use_frameworks!

pod "Hero"
pod "IGCMenu"
pod "ScrollableGraphView"
pod "IQKeyboardManagerSwift", '5.0.0'

end
